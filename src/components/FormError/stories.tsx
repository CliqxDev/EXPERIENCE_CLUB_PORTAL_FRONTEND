import { Meta, Story } from '@storybook/react';

import FormError from '.';

export default {
  title: 'FormError ',
  component: FormError
} as Meta;

export const Default: Story = () => <FormError />;
