import { Meta, Story } from '@storybook/react';

import SlideCarousel from '.';

export default {
  title: 'SlideCarousel ',
  component: SlideCarousel
} as Meta;

export const Default: Story = () => <SlideCarousel />;
