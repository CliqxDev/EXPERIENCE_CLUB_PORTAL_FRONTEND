import { Meta, Story } from '@storybook/react';

import InfoCardColumnist from '.';

export default {
  title: 'InfoCardColumnist ',
  component: InfoCardColumnist
} as Meta;

export const Default: Story = () => <InfoCardColumnist />;
