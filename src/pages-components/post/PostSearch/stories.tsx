import { Meta, Story } from '@storybook/react';

import PostSearch from '.';

export default {
  title: 'PostSearch ',
  component: PostSearch
} as Meta;

export const Default: Story = () => <PostSearch />;
