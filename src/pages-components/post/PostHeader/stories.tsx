import { Meta, Story } from '@storybook/react';

import PostHeader from '.';

export default {
  title: 'PostHeader ',
  component: PostHeader
} as Meta;

export const Default: Story = () => <PostHeader />;
