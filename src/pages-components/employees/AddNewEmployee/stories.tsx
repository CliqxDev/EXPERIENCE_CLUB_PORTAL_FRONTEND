import { Meta, Story } from '@storybook/react';

import AddNewEmployee from '.';

export default {
  title: 'AddNewEmployee ',
  component: AddNewEmployee
} as Meta;

export const Default: Story = () => <AddNewEmployee />;
