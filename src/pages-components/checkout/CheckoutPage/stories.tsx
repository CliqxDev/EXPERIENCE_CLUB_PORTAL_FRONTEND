import { Meta, Story } from '@storybook/react';

import CheckoutPage from '.';

export default {
  title: 'CheckoutPage ',
  component: CheckoutPage
} as Meta;

export const Default: Story = () => <CheckoutPage />;
