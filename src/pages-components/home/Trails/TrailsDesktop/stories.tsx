import { Meta, Story } from '@storybook/react';

import TrailsDesktop from '.';

export default {
  title: 'TrailsDesktop ',
  component: TrailsDesktop
} as Meta;

export const Default: Story = () => <TrailsDesktop />;
