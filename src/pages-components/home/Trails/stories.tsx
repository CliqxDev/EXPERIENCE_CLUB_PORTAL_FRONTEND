import { Meta, Story } from '@storybook/react';
import React from 'react';
import Trails from '.';

export default {
  title: 'Trails ',
  component: Trails
} as Meta;

export const Default: Story = () => <Trails />;
