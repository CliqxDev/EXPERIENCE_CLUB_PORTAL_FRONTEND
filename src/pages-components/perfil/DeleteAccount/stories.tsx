import { Meta, Story } from '@storybook/react';

import DeleteAccount from '.';

export default {
  title: 'DeleteAccount ',
  component: DeleteAccount
} as Meta;

export const Default: Story = () => <DeleteAccount />;
