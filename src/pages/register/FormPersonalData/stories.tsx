import { Meta, Story } from '@storybook/react';

import FormPersonalData from '.';

export default {
  title: 'FormPersonalData ',
  component: FormPersonalData
} as Meta;

export const Default: Story = () => <FormPersonalData />;
