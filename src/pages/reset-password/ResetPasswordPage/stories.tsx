import { Meta, Story } from '@storybook/react';

import ResetPasswordPage from '.';

export default {
  title: 'ResetPasswordPage ',
  component: ResetPasswordPage
} as Meta;

export const Default: Story = () => <ResetPasswordPage />;
